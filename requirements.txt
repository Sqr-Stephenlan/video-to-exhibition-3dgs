# CPU-check / orchestration dependencies for depth-prior L1 tests.
# GPU backends (torch/CUDA, Video Depth Anything) are installed locally per configs/depth/backend_pin.md.
PyYAML>=6.0
numpy>=1.24,<2
Pillow>=10.0
pytest>=7.4
