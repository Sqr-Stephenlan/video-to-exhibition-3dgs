# === video-to-exhibition-3dgs: Python 3.13 + CUDA 12.8 ===
# Install PyTorch first (Blackwell / RTX 5060 requires CUDA 12.8):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Then:
#   pip install -r requirements.txt

# -- 3DGS Training --
gsplat>=1.4.0

# -- Image / Video Processing --
opencv-python>=4.10.0
scikit-image>=0.24.0
imageio>=2.36.0
imageio-ffmpeg>=0.5.0
Pillow>=10.4.0

# -- Scientific Computing --
numpy>=1.26.0
scipy>=1.14.0

# -- Data / Visualisation --
pandas>=2.2.0
matplotlib>=3.9.0
tqdm>=4.66.0
rich>=13.7.0

# -- 3D File Formats --
plyfile>=1.0.0

# -- Video Segmentation (PySceneDetect Python API) --
scenedetect>=0.6.4

# -- Perceptual Hashing (near-duplicate frame detection) --
imagehash>=4.3.0

# -- Depth Estimation (Video-Depth-Anything dependencies) --
transformers>=4.45.0
accelerate>=0.34.0
timm>=1.0.0
einops>=0.8.0
easydict>=1.10

# -- MASt3R / dust3r dependencies --
roma>=1.5.0
huggingface_hub>=0.24.0

# -- Development --
pytest>=8.3.0
ruff>=0.6.0
