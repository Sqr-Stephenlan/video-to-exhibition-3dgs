# Depth prior unit tests

L1 CPU tests for config/manifest validation. Integration/GPU smoke tests remain reserved until sample frames exist.

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/depth -q
```
